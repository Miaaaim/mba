# 服务器 IP 地址（腾讯云公网 IP）
SERVER_HOST=your-server-ip

# SSH 用户名（推荐用 deploy 用户，不要用 root）
SERVER_USER=deploy

# SSH 私钥（从本地生成后粘贴到这里）
# 生成方法: ssh-keygen -t ed25519 -C "github-actions-deploy"
# 然后粘贴 ~/.ssh/deploy_mba_site 的内容
SERVER_SSH_KEY=
