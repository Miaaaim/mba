# Vite 环境变量
# 复制此文件为 .env.production 并填入实际值
# 注意: .env.production 已在 .gitignore 中，不会被提交

# 应用访问 URL（可选）
VITE_APP_URL=https://your-domain.com

# 可选：显式指定构建基路径
# 不填时默认使用相对路径（./），可部署在任意子路径
# 示例：/mba/
VITE_BASE_PATH=

# Gemini API Key（如果项目需要）
# GEMINI_API_KEY=your-key-here
